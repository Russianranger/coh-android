--- a/Common/sql/sqlconn.c
+++ b/Common/sql/sqlconn.c
@@ -34,6 +34,8 @@
     HDBC connections[SQLCONN_MAX];
 
     HENV env;
+    bool unit_active[SQLCONN_MAX];
+    char unit_state[SQLCONN_MAX][6];
 
     char database_string[256];
     char login_string[SHORT_SQL_STRING];
@@ -42,6 +44,27 @@
     LONG stmt_allocs;
 #endif
 } sql;
+
+void sqlConnBeginUnit(SqlConn conn)
+{
+    assert(!sql.unit_active[conn]);
+    sql.unit_state[conn][0] = 0;
+    sql.unit_active[conn] = true;
+}
+void sqlConnFinishUnit(SqlConn conn) { sql.unit_active[conn] = false; }
+bool sqlConnUnitFailed(SqlConn conn) { return sql.unit_state[conn][0] != 0; }
+const char *sqlConnUnitState(SqlConn conn) { return sql.unit_state[conn]; }
+
+static void sqlRecordUnitError(SQLSMALLINT kind, SQLHANDLE handle, SQLRETURN ret, SqlConn conn)
+{
+    SQLINTEGER native;
+    SQLCHAR state[6] = {0};
+    if (!sql.unit_active[conn] || sqlConnUnitFailed(conn) || ret >= 0)
+        return;
+    SQLGetDiagRecA(kind, handle, 1, state, &native, NULL, 0, NULL);
+    // Preserve the first failure; later 25P02 must not hide a retryable error.
+    strcpy(sql.unit_state[conn], state[0] ? (char *)state : "HY000");
+}
 
 static const char* sqlTypeName(SQLSMALLINT type)
 {
@@ -526,6 +549,8 @@
 {
     SQLRETURN ret;
     U32 firstFailTime = 0;
+    if (sql.unit_active[conn] && sqlConnUnitFailed(conn))
+        return SQL_ERROR;
 
     for (;;) {
         U32 now;
@@ -536,6 +561,7 @@
         else
             ret = _sqlConnStmtExecDirectAscii(stmt, str, str_len, conn);
 
+        sqlRecordUnitError(SQL_HANDLE_STMT, stmt, ret, conn);
         if (SQL_SHOW_ERROR(ret))
             sqlConnStmtPrintErrorAndGetState(stmt, str, sql_state);
 
@@ -548,6 +574,9 @@
                 break;
         }
 #else
+        // The queue owns complete-transaction retries while a unit is active.
+        if (sql.unit_active[conn])
+            break;
         // Do not retry on non error conditions or on the foreground thread
         if (!SQL_NEEDS_RETRY(ret) || SQLSTATE_SKIP_RETRY(sql_state) || (conn == SQLCONN_FOREGROUND))
             break;
@@ -586,7 +615,7 @@
         sqlConnStmtPrintError(stmt, str);
 
     if (next < 0)
-        ret = next;
+        return next;
 
 #ifdef FULLDEBUG
     // SQLMoreResults does not function properly if SQL_NEED_DATA was returned
@@ -594,6 +623,7 @@
 #endif
 
     while ((next = SQLMoreResults(stmt)) != SQL_NO_DATA) {
+        sqlRecordUnitError(SQL_HANDLE_STMT, stmt, next, conn);
         if (SQL_SHOW_ERROR(next))
             sqlConnStmtPrintError(stmt, str);
         if (next < 0)
@@ -616,6 +646,7 @@
 
     since_last = timerElapsedAndStart(sql.timers[conn]);
     ret = SQLPrepareA(stmt, (char*)str, str_len);
+    sqlRecordUnitError(SQL_HANDLE_STMT, stmt, ret, conn);
     sqlConnTimerAddTime(conn, since_last);
 
     if (ret != SQL_SUCCESS)
@@ -631,6 +662,7 @@
 
     since_last = timerElapsedAndStart(sql.timers[conn]);
     ret = SQLExecute(stmt);
+    sqlRecordUnitError(SQL_HANDLE_STMT, stmt, ret, conn);
     sqlConnTimerAddTime(conn, since_last);
 
     return ret;
@@ -642,6 +674,7 @@
 
     since_last = timerElapsedAndStart(sql.timers[conn]);
     ret = SQLFetch(stmt);
+    sqlRecordUnitError(SQL_HANDLE_STMT, stmt, ret, conn);
     sqlConnTimerAddTime(conn, since_last);
 
     return ret;
@@ -662,13 +695,15 @@
 
     sqlConnTimerAddTime(conn, since_last);
 
+    sqlRecordUnitError(SQL_HANDLE_DBC, sql.connections[conn], ret, conn);
     if (ret != SQL_SUCCESS)
         sqlConnPrintError(SQL_HANDLE_DBC, sql.connections[conn], "SQLEndTran", NULL);
 
     if (!SQL_SUCCEEDED(ret))
     {
         LOG(LOG_ERROR, LOG_LEVEL_ALERT, LOG_CONSOLE_ALWAYS, "Manual end transaction (commit %d) failed\n", commit);
-        devassert(0);
+        if (!sql.unit_active[conn])
+            devassert(0);
     }
 
     return ret;
@@ -747,7 +782,7 @@
 
         if (!SQL_SUCCEEDED(SQLDriverConnectA(sql.connections[i], 0, (SQLCHAR*)conn_str, conn_str_len, NULL, 0, NULL, SQL_DRIVER_NOPROMPT)))
         {
-            sqlConnPrintErrorf(SQL_HANDLE_DBC, sql.connections[i], "SQLDriverConnect: %s", conn_str);
+            sqlConnPrintErrorf(SQL_HANDLE_DBC, sql.connections[i], "SQLDriverConnect failed (connection string withheld)");
             return false;
         }
     }
@@ -908,6 +943,7 @@
         assert(avail);
 
         ret = SQLGetData(stmt, param, ctype, sb[conn].buff + sb[conn].idx, avail, &got);
+        sqlRecordUnitError(SQL_HANDLE_STMT, stmt, ret, conn);
         if (!SQL_SUCCEEDED(ret))
             break;
 
--- a/Common/sql/sqlconn.h
+++ b/Common/sql/sqlconn.h
@@ -39,6 +39,12 @@
 bool sqlConnStmtPrintError(HSTMT stmt, const char *original_command);
 bool sqlConnStmtPrintErrorf(HSTMT stmt, char *fmt, ...);
 
+// An atomic unit owns retries; statement-level retries are disabled inside it.
+void sqlConnBeginUnit(SqlConn conn);
+void sqlConnFinishUnit(SqlConn conn);
+bool sqlConnUnitFailed(SqlConn conn);
+const char *sqlConnUnitState(SqlConn conn);
+
 // transactions
 bool sqlConnEnableAsynchDBC(bool enable, SqlConn conn);
 bool sqlConnEnableAutoTransactions(bool enable, SqlConn conn);
--- a/DBServer/CMakeLists.txt
+++ b/DBServer/CMakeLists.txt
@@ -163,6 +163,11 @@
 
 add_executable(DbServer
 	${DbServer_SOURCES} ${DbServer_HEADERS} ${DbServer_GENERATED})
+option(COH_PG_PERSISTENCE_TESTS "Enable disposable PostgreSQL fixture entry point" OFF)
+if(COH_PG_PERSISTENCE_TESTS)
+    target_sources(DbServer PRIVATE src/pg_persistence_test.c)
+    target_compile_definitions(DbServer PRIVATE COH_PG_PERSISTENCE_TESTS)
+endif()
 source_group(TREE "${PROJECT_SOURCE_DIR}" FILES
 	${DbServer_SOURCES} ${DbServer_HEADERS} ${DbServer_GENERATED})
 cox_target_defaults(DbServer)
--- a/DBServer/src/auctionservercomm.c
+++ b/DBServer/src/auctionservercomm.c
@@ -195,7 +195,10 @@
             days_active = server_cfg.auction_last_login_delay;
             auction_state.sqlcb_link = auc_link;
 
-            estrPrintf(&restriction,"WHERE LastActive > DATEADD(d, -%i, GETDATE())", days_active);
+            if (gDatabaseProvider == DBPROV_POSTGRESQL)
+                estrPrintf(&restriction, "WHERE LastActive > LOCALTIMESTAMP - (%i * INTERVAL '1 day')", days_active);
+            else
+                estrPrintf(&restriction,"WHERE LastActive > DATEADD(d, -%i, GETDATE())", days_active);
             sqlReadColumnsAsync(ent_list->tplt->tables,0,"ContainerId",restriction,sqlAuctionGetActiveCallback,0,pak_out,0);
 
             estrDestroy(&restriction);
--- a/DBServer/src/clientcomm.c
+++ b/DBServer/src/clientcomm.c
@@ -2618,7 +2618,10 @@
         cbData = calloc(1, sizeof(UnlockCharacterCbData));
         cbData->client = client;
         cbData->ent_id = ent_id;
-        sprintf(buf, "WHERE AuthName='%s' AND IsSlotLocked IS NULL", client->account_name); // can't use AuthId in dev environment
+        if (gDatabaseProvider == DBPROV_POSTGRESQL)
+            sprintf(buf, "WHERE dbo.coh_name_key(AuthName)=dbo.coh_name_key('%s') AND IsSlotLocked IS NULL", client->account_name);
+        else
+            sprintf(buf, "WHERE AuthName='%s' AND IsSlotLocked IS NULL", client->account_name); // can't use AuthId in dev environment
         sqlReadColumnsAsync(ent_list->tplt->tables,0,"ContainerId",buf,s_finishHandleUnlockCharacter,cbData,NULL,0);
     }
 }
--- a/DBServer/src/container.c
+++ b/DBServer/src/container.c
@@ -17,6 +17,8 @@
 #include "dblog.h"
 #include "namecache.h"
 #include "container_tplt.h"
+#include "sql/pg_compat.h"
+#include <limits.h>
 #include <utilitieslib/components/EString.h>
 #include "dbperf.h"
 #include "dbmsg.h" // for containerBroadcast
@@ -700,12 +702,14 @@
                     xcase DBPROV_MSSQL:
                         cmd_len = sprintf(cmd, "SELECT IDENT_CURRENT('%s');", list->tplt->tables->name);
                     xcase DBPROV_POSTGRESQL:
-                        cmd_len = sprintf(cmd, "SELECT setval(pg_get_serial_sequence('dbo.%s', 'containerid'), nextval(pg_get_serial_sequence('dbo.%s', 'containerid')), false);", list->tplt->tables->name, list->tplt->tables->name);
+                        cmd_len = sprintf(cmd, COH_PG_HIGH_WATER, list->tplt->tables->name);
                     DBPROV_XDEFAULT();
                 }
 
                 list->max_container_id = sqlGetSingleValue(cmd, cmd_len, NULL, SQLCONN_FOREGROUND);
             }
+            if (list->max_container_id == INT_MAX)
+                FatalErrorf("Container ID range exhausted for %s", list->tplt->tables->name);
             idx = ++list->max_container_id;
         }
         else
@@ -1039,7 +1043,11 @@
     bind_list[0].data_type = CFTYPE_ANSISTRING;
     bind_list[0].data = value;
 
-    buf_len = sprintf(buf, "SELECT ContainerId FROM dbo.%s WHERE %s = ?;", list->tplt->tables->name, element);
+    if (gDatabaseProvider == DBPROV_POSTGRESQL &&
+        (!stricmp(element, "Name") || !stricmp(element, "AuthName")))
+        buf_len = sprintf(buf, "SELECT ContainerId FROM dbo.%s WHERE dbo.coh_name_key(%s) = dbo.coh_name_key(?);", list->tplt->tables->name, element);
+    else
+        buf_len = sprintf(buf, "SELECT ContainerId FROM dbo.%s WHERE %s = ?;", list->tplt->tables->name, element);
     return sqlGetSingleValue(buf, buf_len, bind_list, SQLCONN_FOREGROUND);
 }
 
--- a/DBServer/src/container_sql.c
+++ b/DBServer/src/container_sql.c
@@ -3,6 +3,7 @@
 #include "sql/sqlinclude.h"
 #include <utilitieslib/utils/utils.h>
 #include "container_sql.h"
+#include "sql/pg_compat.h"
 #include "container_diff.h"
 #include "container_merge.h"
 #include <utilitieslib/utils/timing.h>
@@ -203,7 +204,7 @@
         xcase DBPROV_MSSQL:
             buf_len = sprintf(buf, "IF NOT EXISTS (SELECT constraint_name FROM information_schema.table_constraints WHERE constraint_name = 'FK_%s_%s_%s') ALTER TABLE dbo.%s ADD CONSTRAINT FK_%s_%s_%s FOREIGN KEY (%s) REFERENCES %s;", table, key, foreign_table, table, table, key, foreign_table, key, foreign_table);
         xcase DBPROV_POSTGRESQL:
-            buf_len = sprintf(buf, "DO $$BEGIN IF NOT EXISTS (SELECT constraint_name FROM information_schema.table_constraints WHERE constraint_name = 'fk_%s_%s_%s') THEN ALTER TABLE dbo.%s ADD CONSTRAINT FK_%s_%s_%s FOREIGN KEY (%s) REFERENCES dbo.%s; END IF; END$$;", strlwrdupa(table), strlwrdupa(key), strlwrdupa(foreign_table), table, table, key, foreign_table, key, foreign_table);
+            buf_len = sprintf(buf, COH_PG_ADD_FK, table, table, key, foreign_table, table, table, key, foreign_table, key, foreign_table);
         DBPROV_XDEFAULT();
     }
     sqlExecAsync(buf, buf_len);
@@ -221,7 +222,7 @@
         xcase DBPROV_MSSQL:
             buf_len = sprintf(buf, "IF EXISTS (SELECT constraint_name FROM information_schema.table_constraints WHERE constraint_name = 'FK_%s_%s_%s') ALTER TABLE dbo.%s DROP CONSTRAINT FK_%s_%s_%s;", table, key, foreign_table, table, table, key, foreign_table);
         xcase DBPROV_POSTGRESQL:
-            buf_len = sprintf(buf, "DO $$BEGIN IF EXISTS (SELECT constraint_name FROM information_schema.table_constraints WHERE constraint_name = 'fk_%s_%s_%s') THEN ALTER TABLE dbo.%s DROP CONSTRAINT FK_%s_%s_%s; END IF; END$$;", strlwrdupa(table), strlwrdupa(key), strlwrdupa(foreign_table), table, table, key, foreign_table);
+            buf_len = sprintf(buf, COH_PG_DROP_FK, table, table, key, foreign_table);
         DBPROV_XDEFAULT();
     }
     
@@ -246,7 +247,7 @@
         xcase DBPROV_MSSQL:
             buf_len = sprintf(buf, "IF NOT EXISTS (SELECT sys.indexes.name FROM sys.indexes JOIN sys.objects on sys.indexes.object_id=sys.objects.object_id WHERE sys.indexes.name = N'%s' and sys.objects.name=N'%s') CREATE INDEX %s ON dbo.%s (%s);", index, table, index, table, fields);
         xcase DBPROV_POSTGRESQL:
-            buf_len = sprintf(buf, "DO $$BEGIN IF NOT EXISTS (SELECT relname FROM pg_catalog.pg_class JOIN pg_catalog.pg_index ON pg_catalog.pg_class.oid = pg_catalog.pg_index.indexrelid WHERE relname = '%s' AND indrelid = '%s'::regclass::oid) THEN CREATE INDEX %s ON dbo.%s (%s); END IF; END$$;", strlwrdupa(index), strlwrdupa(table), index, table, fields);
+            buf_len = sprintf(buf, COH_PG_ADD_INDEX, table, index, table, fields);
         DBPROV_XDEFAULT();
     }
 
@@ -269,7 +270,7 @@
         xcase DBPROV_MSSQL:
             buf_len = sprintf(buf, "IF EXISTS (SELECT sys.indexes.name FROM sys.indexes JOIN sys.objects on sys.indexes.object_id=sys.objects.object_id WHERE sys.indexes.name = N'%s' and sys.objects.name=N'%s') DROP INDEX %s ON dbo.%s;", index, table, index, table);
         xcase DBPROV_POSTGRESQL:
-            buf_len = sprintf(buf, "DO $$BEGIN IF EXISTS (SELECT relname FROM pg_catalog.pg_class JOIN pg_catalog.pg_index ON pg_catalog.pg_class.oid = pg_catalog.pg_index.indexrelid WHERE relname = '%s' AND indrelid = '%s'::regclass::oid) THEN DROP INDEX %s ON dbo.%s; END IF; END$$;", strlwrdupa(index), strlwrdupa(table), index, table);
+            buf_len = sprintf(buf, COH_PG_DROP_INDEX, table, table, index, index);
         DBPROV_XDEFAULT();
     }
 
@@ -292,6 +293,15 @@
 
     if (!tplt || tplt->dont_write_to_sql)
         return;
+
+    if (gDatabaseProvider == DBPROV_POSTGRESQL) {
+        char *cmd = NULL;
+        for (i=tplt->table_count-1; i>=0; --i)
+            estrConcatf(&cmd, "DELETE FROM dbo.%s WHERE ContainerId=%d;", tplt->tables[i].name, container_id);
+        sqlExecAsyncEx(cmd, estrLength(&cmd), container_id, false);
+        estrDestroy(&cmd);
+        return;
+    }
 
     for(i=tplt->table_count-1;i>=0;i--)
     {
@@ -335,7 +345,7 @@
                             estrConcatf(estr, "SET IDENTITY_INSERT dbo.%s ON; INSERT INTO dbo.%s (ContainerId) VALUES (?); SET IDENTITY_INSERT dbo.%s OFF;", name, name, name);
                             bindInputParameter(stmt, (*bind)++, CFTYPE_INT, container_id, NULL);
                         xcase DBPROV_POSTGRESQL:
-                            estrConcatf(estr, "SELECT setval(pg_get_serial_sequence('dbo.%s', 'containerid'), ?); INSERT INTO dbo.%s (ContainerId) VALUES (?);", name, name);
+                            estrConcatf(estr, COH_PG_INSERT_CONTAINER, name, name);
                             bindInputParameter(stmt, (*bind)++, CFTYPE_INT, container_id, NULL);
                             bindInputParameter(stmt, (*bind)++, CFTYPE_INT, container_id, NULL);
                         DBPROV_XDEFAULT();
@@ -1050,6 +1060,7 @@
     if (!SQL_SUCCEEDED(retcode))
     {
         *count_ptr = 0;
+        sqlConnStmtFree(stmt);
         return 0;
     }
 
@@ -1180,6 +1191,30 @@
         columns[count].data_type = CFTYPE_NULL;
         columns[count].num_bytes = buffer_length;
         columns[count].column_size = 0;
+
+        if (gDatabaseProvider == DBPROV_POSTGRESQL)
+        {
+            /* PostgreSQL has no tinyint or separate nvarchar, and SQLColumns
+             * display sizes are not CoH's in-memory buffer sizes. */
+            CohPgKind kind = cohPgColumnType((char *)type_name, column_size,
+                columns[count].data_type_name, sizeof(columns[count].data_type_name));
+            switch (kind) {
+                case COH_PG_SHORT: columns[count].data_type = CFTYPE_SHORT; break;
+                case COH_PG_INT: columns[count].data_type = CFTYPE_INT; break;
+                case COH_PG_FLOAT: columns[count].data_type = CFTYPE_FLOAT; break;
+                case COH_PG_STRING: columns[count].data_type = CFTYPE_UNICODESTRING; break;
+                case COH_PG_TIMESTAMP: columns[count].data_type = CFTYPE_DATETIME; break;
+                case COH_PG_TEXT: columns[count].data_type = CFTYPE_UNICODESTRING_MAX; break;
+                case COH_PG_BINARY: columns[count].data_type = CFTYPE_BINARY_MAX; break;
+                default: FatalErrorf("Unsupported PostgreSQL type %s in %s.%s", type_name, table_name, column_name);
+            }
+            columns[count].num_bytes = g_containerfieldinfo[columns[count].data_type].access_size;
+            if (kind == COH_PG_STRING) {
+                columns[count].column_size = column_size;
+                columns[count].num_bytes = (column_size + 1) * 2;
+            }
+            continue;
+        }
 
         for (i = 1; i < CFTYPE_COUNT; i++)
         {
--- a/DBServer/src/container_tplt.c
+++ b/DBServer/src/container_tplt.c
@@ -323,7 +323,10 @@
         if (new_field->data_type == CFTYPE_TEXTBLOB || new_field->data_type == CFTYPE_BLOB)
             continue;
 
-        assert(new_field->num_bytes >= field->num_bytes);
+        // SQL storage matches above; PG int1/int2 share smallint and ANSI/Unicode
+        // share varchar. Their ODBC buffer sizes cannot validate CoH's buffers.
+        if (gDatabaseProvider != DBPROV_POSTGRESQL)
+            assert(new_field->num_bytes >= field->num_bytes);
         assert(new_field->column_size == field->column_size);
     }
 
@@ -490,7 +493,7 @@
     table.columns[0].reserved_word = 1;
 
     // Mark the SubId as reserved if this table has one
-    if (strcmp(table.columns[1].name,"SubId")==0)
+    if (stricmp(table.columns[1].name,"SubId")==0)
     {
         table.array_count = 1;
         table.columns[1].reserved_word = 1;
@@ -513,6 +516,56 @@
     }
 
     return sqlTableReadMulti(tplt, &table, -1, multi_buf_ptr, SQLCONN_FOREGROUND);
+}
+
+static bool postgresNeedsRebuild(TableInfo *table)
+{
+    ColumnInfo *old = NULL;
+    int count = sqlGetTableInfo(table->name, &old), i;
+    bool changed = count && count != table->num_columns;
+    for (i=0; count && i<count && i<table->num_columns && !changed; ++i)
+        changed = stricmp(old[i].name,table->columns[i].name) ||
+                  stricmp(old[i].data_type_name,table->columns[i].data_type_name);
+    free(old);
+    return changed;
+}
+
+static bool rebuildPostgresTable(TableInfo *table)
+{
+    ColumnInfo *old = NULL;
+    char *definition = NULL, *columns = NULL;
+    int count, i, j;
+    bool ok;
+    // Startup-only: finish every queued write/index operation before DDL.
+    sqlFifoBarrier();
+    sqlFifoFinish();
+    count = sqlGetTableInfo(table->name, &old);
+    for (i=0; i<table->num_columns; ++i) {
+        ColumnInfo *field = &table->columns[i];
+        if (i) estrConcatCharString(&definition, ",");
+        if (table->table_type == TT_CONTAINER && !stricmp(field->name, "ContainerId"))
+            estrConcatf(&definition, "%s SERIAL NOT NULL", field->name);
+        else
+            estrConcatf(&definition, "%s %s%s", field->name, field->data_type_name,
+                (!stricmp(field->name,"ContainerId") || !stricmp(field->name,"SubId")) ? " NOT NULL" : "");
+        for (j=0; j<count; ++j) {
+            if (!stricmp(field->name, old[j].name)) {
+                if (estrLength(&columns)) estrConcatCharString(&columns, ",");
+                estrConcatCharString(&columns, field->name);
+                break;
+            }
+        }
+    }
+    estrConcatf(&definition, ",PRIMARY KEY (ContainerId%s)", table->table_type==TT_SUBCONTAINER ? ",SubId" : "");
+    ok = sqlExecDdlf(DDL_REBUILDTABLE,
+        "SELECT dbo.coh_rebuild_table('dbo.%s'::regclass, '%s', '%s', %s);",
+        table->name, definition, columns, table->table_type==TT_CONTAINER ? "true" : "false");
+    free(old);
+    estrDestroy(&definition);
+    estrDestroy(&columns);
+    if (!ok)
+        FatalErrorf("PostgreSQL rebuild of %s failed; original table preserved. Fix the schema before restarting.", table->name);
+    return updateTable(table);
 }
 
 static bool rebuildTableDataInPlace(ContainerTemplate *tplt, TableInfo *table)
@@ -526,6 +579,9 @@
     int old_count;
     bool ret;
 
+    if (gDatabaseProvider == DBPROV_POSTGRESQL)
+        return rebuildPostgresTable(table);
+
     // Setup the temporary table
     memcpy(&tmpTable, table, sizeof(TableInfo));
     strcat(tmpTable.name, "_tmp");
@@ -549,7 +605,7 @@
     estrSetLength(&columns, estrLength(&columns)-1);
     assert(estrLength(&columns));
 
-    if (table->table_type == TT_CONTAINER)
+    if (table->table_type == TT_CONTAINER && gDatabaseProvider == DBPROV_MSSQL)
     {
         sprintf(identity_insert_on, " SET IDENTITY_INSERT dbo.%s_tmp ON;", table->name);
         sprintf(identity_insert_off, " SET IDENTITY_INSERT dbo.%s_tmp OFF;", table->name);
@@ -568,7 +624,7 @@
         // Create new table
         updateTable(table);
 
-        if (table->table_type == TT_CONTAINER)
+        if (table->table_type == TT_CONTAINER && gDatabaseProvider == DBPROV_MSSQL)
         {
             sprintf(identity_insert_on, " SET IDENTITY_INSERT dbo.%s ON;", table->name);
             sprintf(identity_insert_off, " SET IDENTITY_INSERT dbo.%s OFF;", table->name);
@@ -674,10 +730,13 @@
 
     for(j=0;j<tplt->table_count;j++)
     {
-        if (!updateTable(&tplt->tables[j]))
+        if (!(gDatabaseProvider == DBPROV_POSTGRESQL && postgresNeedsRebuild(&tplt->tables[j])
+              ? rebuildPostgresTable(&tplt->tables[j]) : updateTable(&tplt->tables[j])))
         {
             if(!rebuildTableDataInPlace(tplt, &tplt->tables[j]))
             {
+                if (gDatabaseProvider == DBPROV_POSTGRESQL)
+                    FatalErrorf("PostgreSQL schema verification failed for %s; refusing destructive fallback", tplt->tables[j].name);
                 static bool s_oktoall = false;
                 if (!s_oktoall) {
                     switch(okToAllCancelDialog("Falling back to the old method for rebuilding a table.\r\n\r\n"
--- a/DBServer/src/container_tplt_utils.c
+++ b/DBServer/src/container_tplt_utils.c
@@ -10,6 +10,7 @@
 #include <utilitieslib/utils/error.h>
 #include <utilitieslib/utils/wininclude.h>
 #include "sql/sqlconn.h"
+#include "sql/pg_compat.h"
 #include <utilitieslib/utils/log.h>
 #include <utilitieslib/utils/SuperAssert.h>
 
@@ -352,6 +353,9 @@
 
     printf_stderr("SQL_GETDATA_EXTENSIONS = 0x%08x\n", sqlConnGetInfo(SQL_GETDATA_EXTENSIONS, false, SQLCONN_FOREGROUND));
 
+    if (gDatabaseProvider == DBPROV_POSTGRESQL)
+        g_containerfieldinfo[CFTYPE_BYTE].access_type = COH_PG_BYTE_CTYPE;
+
     // Verify connection timeout
     timeout = sqlConnGetInfo(SQL_ATTR_QUERY_TIMEOUT, false, SQLCONN_FOREGROUND);
     assertmsgf(!timeout, "SQL server is set to timeout after %d seconds", timeout);
@@ -382,7 +386,7 @@
         xcase DBPROV_MSSQL:
             cmd_len = sprintf(cmd, "SELECT compatibility_level FROM sys.databases WHERE name=db_name();");
         xcase DBPROV_POSTGRESQL:
-            cmd_len = sprintf(cmd, "SELECT CAST(CAST(substring(character_value FROM '[[:digit:]]+\\.[[:digit:]]+') AS float)*100 as integer) FROM information_schema.sql_implementation_info WHERE implementation_info_name = 'DBMS VERSION';");
+            cmd_len = sprintf(cmd, "SELECT current_setting('server_version_num')::integer;");
         DBPROV_XDEFAULT();
     }
 
--- a/DBServer/src/dbdispatch.c
+++ b/DBServer/src/dbdispatch.c
@@ -357,7 +357,10 @@
     if (!findFieldText(str, "AuthName", authName))
         return 0;
 
-    sprintf(restriction, "WHERE AuthName='%s'", authName);
+    if (gDatabaseProvider == DBPROV_POSTGRESQL)
+        sprintf(restriction, "WHERE dbo.coh_name_key(AuthName)=dbo.coh_name_key('%s')", authName);
+    else
+        sprintf(restriction, "WHERE AuthName='%s'", authName);
     rows = sqlReadColumnsSlow(ent_list->tplt->tables,0,"ContainerId, Name",restriction, &row_count, field_ptrs);
     if (!rows)
         return 1;
@@ -2397,7 +2400,7 @@
     }
 
     // now destroy the container
-    cmd_len = sprintf(cmd, "SELECT ContainerId FROM Base WHERE ISNULL(SupergroupId,0) = %d AND ISNULL(UserId,0) = %d;", sg_id, user_id);
+    cmd_len = sprintf(cmd, "SELECT ContainerId FROM Base WHERE COALESCE(SupergroupId,0) = %d AND COALESCE(UserId,0) = %d;", sg_id, user_id);
     container_id = sqlGetSingleValue(cmd, cmd_len, NULL, SQLCONN_FOREGROUND);
 
     container = containerPtr(dbListPtr(CONTAINER_BASE), container_id);
--- a/DBServer/src/dbimport.c
+++ b/DBServer/src/dbimport.c
@@ -148,7 +148,7 @@
                     EntCatalogInfo *entInfo = entCatalogGet( id, false );    assert(entInfo);
                     authId = ( entInfo ) ? entInfo->authId : 0;
                     sqlFifoBarrier();
-                    sprintf(cmd,"UPDATE dbo.Ents SET DbFlags = ISNULL(DbFlags,0) | 4096 WHERE ContainerId = %d;",id); // DBFLAG_RENAMEABLE
+                    sprintf(cmd,"UPDATE dbo.Ents SET DbFlags = COALESCE(DbFlags,0) | 4096 WHERE ContainerId = %d;",id); // DBFLAG_RENAMEABLE
                     sqlConnExecDirect(cmd, SQL_NTS, SQLCONN_FOREGROUND, false);
                     sqlFifoBarrier();
                     sprintf(cmd,"UPDATE dbo.Ents SET Name = N'%s' WHERE ContainerId = %d;", new_name, id);
--- a/DBServer/src/dbinit.c
+++ b/DBServer/src/dbinit.c
@@ -1085,6 +1085,8 @@
     sqlConnSetLogin(server_cfg.sql_login);
     if (!sqlConnDatabaseConnect(server_cfg.sql_db_name, "DBSERVER"))
     {
+        if (gDatabaseProvider == DBPROV_POSTGRESQL)
+            FatalErrorf("PostgreSQL connection failed. Initialize the database with database/postgresql/pg_local.py and check SqlLogin/SqlDbName.");
         sqlCheckDdl(DDL_ADD);
         if (sqlConnDatabaseConnect(NULL, "DBSERVER_INIT"))
             sqlExecDdl(DDL_ADD, server_cfg.sql_init, SQL_NTS);
@@ -1094,6 +1096,9 @@
 
     odbcInitialSetup();
     queryDatabaseVersion();
+    if (gDatabaseProvider == DBPROV_POSTGRESQL &&
+        sqlGetSingleValue("SELECT count(*) FROM coh_meta.schema_version WHERE version=2;", SQL_NTS, NULL, SQLCONN_FOREGROUND) != 1)
+        FatalErrorf("PostgreSQL compatibility migration 2 is required. Stop DbServer and run pg_local.py migrate before restarting.");
 
     printf_stderr("%s SQL server %d connected\n", timerGetTimeString(), gDatabaseVersion);
     sqlFifoInit();
@@ -1762,6 +1767,12 @@
     int        i,start_static=0,flat_to_sql=0,is_log_server=0,init_encryption = 1;
 
     memCheckInit();
+#ifdef COH_PG_PERSISTENCE_TESTS
+    if (argc > 1 && !strcmp(argv[1], "-pgpersistencetest")) {
+        extern int pgPersistenceTestMain(int argc, char **argv);
+        return pgPersistenceTestMain(argc, argv);
+    }
+#endif
 
     EXCEPTION_HANDLER_BEGIN
     
--- a/DBServer/src/sql_fifo.c
+++ b/DBServer/src/sql_fifo.c
@@ -1,4 +1,6 @@
 #include "sql_fifo.h"
+#include "sql/sqlinclude.h"
+#include <process.h>
 #include <utilitieslib/utils/mathutil.h>
 #include <utilitieslib/network/netio.h>
 #include <utilitieslib/utils/error.h>
@@ -681,23 +683,11 @@
     worker->using_transactions = sqlfifo.use_transactions;
 }
 
-static void * runQueueEntry(void * data, void * user)
-{
-    SqlQueueEntry * entry = (SqlQueueEntry*)data;
-    sqlFifoWorker * worker = (sqlFifoWorker*)user;
-
-    if(worker->using_transactions != sqlfifo.use_transactions)
-    {
-        workerStartUsingManualTransactions(worker);
-    }
-
-    if(worker->transaction_in_progress)
-    {
-        // Synchronous wait for end of transaction
-        sqlConnEndTransaction(true, true, worker->conn);
-        worker->transaction_in_progress = 0;
-    }
-
+static volatile LONG pg_retries;
+int sqlFifoRetryCount(void) { return (int)pg_retries; }
+
+static void runQueueCommand(SqlQueueEntry *entry, sqlFifoWorker *worker)
+{
     switch(entry->cmd)
     {
         case SQLCMD_BARRIER:
@@ -738,6 +728,84 @@
             break;
         }
     }
+
+}
+
+static void pgQueueFailClosed(SqlQueueEntry *entry, const char *state)
+{
+    // Do not run atexit FIFO flushing from a worker, or acknowledge an uncertain
+    // save. The supervisor/operator must restart after fixing the cause.
+    fprintf(stderr, "PG_FIFO_FAILED command=%d SQLSTATE=%s; save not acknowledged\n", entry->cmd, state);
+    fflush(stderr);
+    _exit(3);
+}
+
+static void *runPostgresQueueEntry(SqlQueueEntry *entry, sqlFifoWorker *worker)
+{
+    unsigned attempt;
+    if (entry->cmd == SQLCMD_BARRIER) {
+        sql_task_worker_barrier();
+        return entry;
+    }
+    if (!worker->using_transactions) {
+        if (!sqlConnEnableAutoTransactions(false, worker->conn))
+            pgQueueFailClosed(entry, "HY000");
+        worker->using_transactions = true;
+    }
+    for (attempt = 0; attempt < 5; ++attempt) {
+        char state[6];
+        bool retry;
+        sqlConnBeginUnit(worker->conn);
+        runQueueCommand(entry, worker);
+        if (!sqlConnUnitFailed(worker->conn))
+            sqlConnEndTransaction(true, true, worker->conn);
+        if (!sqlConnUnitFailed(worker->conn)) {
+            sqlConnFinishUnit(worker->conn);
+            return entry; // Completion becomes visible only after commit.
+        }
+        strcpy(state, sqlConnUnitState(worker->conn));
+        retry = !strcmp(state, "40001") || !strcmp(state, "40P01");
+        if (!SQL_SUCCEEDED(sqlConnEndTransaction(false, true, worker->conn)))
+            retry = false;
+        sqlConnFinishUnit(worker->conn);
+        if (!retry || attempt == 4)
+            pgQueueFailClosed(entry, state);
+        if (entry->cmd == SQLCMD_READCONTAINER) {
+            free(entry->readcontainer.data);
+            entry->readcontainer.data = NULL;
+        } else if (entry->cmd == SQLCMD_READCOLUMNS) {
+            free(entry->readcolumns.cols);
+            entry->readcolumns.cols = NULL;
+            entry->readcolumns.col_count = 0;
+        }
+        InterlockedIncrement(&pg_retries);
+        printf_stderr("PG_FIFO_RETRY command=%d SQLSTATE=%s attempt=%u\n", entry->cmd, state, attempt + 1);
+        Sleep(25 * (attempt + 1));
+    }
+    return NULL; // Unreachable: last failure terminates without acknowledgement.
+}
+
+static void * runQueueEntry(void * data, void * user)
+{
+    SqlQueueEntry * entry = (SqlQueueEntry*)data;
+    sqlFifoWorker * worker = (sqlFifoWorker*)user;
+
+    if (gDatabaseProvider == DBPROV_POSTGRESQL)
+        return runPostgresQueueEntry(entry, worker);
+
+    if(worker->using_transactions != sqlfifo.use_transactions)
+    {
+        workerStartUsingManualTransactions(worker);
+    }
+
+    if(worker->transaction_in_progress)
+    {
+        // Synchronous wait for end of transaction
+        sqlConnEndTransaction(true, true, worker->conn);
+        worker->transaction_in_progress = 0;
+    }
+
+    runQueueCommand(entry, worker);
 
     if(worker->using_transactions)
     {
--- a/DBServer/src/sql_fifo.h
+++ b/DBServer/src/sql_fifo.h
@@ -22,6 +22,7 @@
 void sqlFifoFinish(void);
 void sqlFifoBarrier(void);
 int sqlFifoWorstQueueDepth(void);
+int sqlFifoRetryCount(void);
 void sqlFifoEnableTransacted(int enable);
 
 // HACK
